@extends('parent.layout')
@section('title', 'Paket dan Pembayaran')
@section('content')
<div class="space-y-6"><div><h1 class="text-2xl font-bold text-gray-900">Paket & Pembayaran</h1><p class="mt-1 text-sm text-gray-500">Status paket belajar serta transaksi {{ $child?->name ?? 'anak' }}.</p></div>
@if($child)<section class="grid gap-4 md:grid-cols-2 xl:grid-cols-3">@forelse($accesses as $access)<article class="rounded-2xl border border-gray-200 bg-white p-5 shadow-sm"><div class="flex justify-between gap-3"><h2 class="font-bold text-gray-900">{{ $access->package?->name ?? 'Paket belajar' }}</h2><span class="rounded-full px-2.5 py-1 text-xs font-semibold {{ $access->is_active ? 'bg-emerald-100 text-emerald-700' : 'bg-gray-100 text-gray-600' }}">{{ $access->is_active ? 'Aktif' : 'Tidak aktif' }}</span></div><dl class="mt-4 space-y-2 text-sm"><div class="flex justify-between gap-3"><dt class="text-gray-500">Masa aktif</dt><dd class="text-right text-gray-800">{{ $access->end_date?->translatedFormat('d M Y') ?? 'Tanpa batas' }}</dd></div><div class="flex justify-between gap-3"><dt class="text-gray-500">Sisa hari</dt><dd class="font-semibold text-gray-800">{{ $access->days_remaining ?? '—' }}</dd></div><div class="flex justify-between gap-3"><dt class="text-gray-500">Sesi booking</dt><dd class="text-gray-800">{{ $access->completed_booking_count }} tercatat</dd></div></dl></article>@empty<div class="rounded-2xl border border-dashed border-gray-300 bg-white p-10 text-center text-sm text-gray-500 md:col-span-2 xl:col-span-3">Belum ada paket untuk anak ini.</div>@endforelse</section>@if($accesses instanceof \Illuminate\Pagination\AbstractPaginator){{ $accesses->links() }}@endif
<section class="overflow-hidden rounded-2xl border border-gray-200 bg-white shadow-sm"><div class="border-b border-gray-100 px-5 py-5"><h2 class="font-bold text-gray-900">Riwayat pembayaran</h2></div><div class="overflow-x-auto"><table class="w-full min-w-[620px] text-left text-sm"><thead class="bg-gray-50 text-xs uppercase text-gray-500"><tr><th class="px-5 py-3">Tanggal</th><th class="px-5 py-3">Paket</th><th class="px-5 py-3">Nominal</th><th class="px-5 py-3">Status</th></tr></thead><tbody class="divide-y divide-gray-100">@forelse($payments as $payment)<tr><td class="px-5 py-4 text-gray-600">{{ $payment->created_at?->translatedFormat('d M Y') }}</td><td class="px-5 py-4 font-medium text-gray-900">{{ $payment->package?->name ?? '—' }}</td><td class="px-5 py-4 text-gray-700">Rp {{ number_format((int) $payment->total_amount, 0, ',', '.') }}</td><td class="px-5 py-4"><span class="rounded-full bg-gray-100 px-2.5 py-1 text-xs font-semibold text-gray-700">{{ ucfirst($payment->status) }}</span></td></tr>@empty<tr><td colspan="4" class="px-5 py-10 text-center text-gray-500">Belum ada transaksi.</td></tr>@endforelse</tbody></table></div>@if($payments instanceof \Illuminate\Pagination\AbstractPaginator){{ $payments->links() }}@endif</section>@endif</div>
@endsection
