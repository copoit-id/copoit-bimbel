@extends('parent.layout')
@section('title', 'Presensi Anak')
@section('content')
<div class="space-y-6"><div><h1 class="text-2xl font-bold text-gray-900">Presensi {{ $child?->name ?? 'Anak' }}</h1><p class="mt-1 text-sm text-gray-500">Riwayat kehadiran pada sesi belajar.</p></div>
@if($child)<section class="grid gap-4 sm:grid-cols-4">@foreach(['total' => 'Total sesi', 'present' => 'Hadir', 'late' => 'Terlambat', 'absent' => 'Alpa'] as $key => $label)<article class="rounded-xl border border-gray-200 bg-white p-4 shadow-sm"><p class="text-sm text-gray-500">{{ $label }}</p><p class="mt-2 text-2xl font-bold text-gray-900">{{ $attendanceSummary[$key] }}</p></article>@endforeach</section><section class="overflow-hidden rounded-2xl border border-gray-200 bg-white shadow-sm"><div class="overflow-x-auto"><table class="w-full min-w-[650px] text-left text-sm"><thead class="bg-gray-50 text-xs uppercase text-gray-500"><tr><th class="px-5 py-3">Tanggal</th><th class="px-5 py-3">Sesi</th><th class="px-5 py-3">Tutor / Rombel</th><th class="px-5 py-3">Status</th><th class="px-5 py-3">Catatan</th></tr></thead><tbody class="divide-y divide-gray-100">@forelse($attendances as $attendance)<tr><td class="px-5 py-4 text-gray-600">{{ $attendance->session?->start_at?->translatedFormat('d M Y · H:i') ?? '—' }}</td><td class="px-5 py-4 font-medium text-gray-900">{{ $attendance->session?->schedule?->title ?? 'Sesi kelas' }}</td><td class="px-5 py-4 text-gray-600">{{ $attendance->session?->tentor?->name ?? '—' }}<br><span class="text-xs text-gray-400">{{ $attendance->session?->studyGroup?->name }}</span></td><td class="px-5 py-4"><span class="rounded-full px-2.5 py-1 text-xs font-semibold {{ in_array($attendance->status, ['present','late']) ? 'bg-emerald-100 text-emerald-700' : ($attendance->status === 'absent' ? 'bg-red-100 text-red-700' : 'bg-amber-100 text-amber-700') }}">{{ ['present'=>'Hadir','late'=>'Terlambat','absent'=>'Alpa','excused'=>'Izin'][$attendance->status] ?? $attendance->status }}</span></td><td class="px-5 py-4 text-gray-600">{{ $attendance->notes ?: '—' }}</td></tr>@empty<tr><td colspan="5" class="px-5 py-12 text-center text-gray-500">Belum ada riwayat presensi.</td></tr>@endforelse</tbody></table></div>@if($attendances instanceof \Illuminate\Pagination\AbstractPaginator){{ $attendances->links() }}@endif</section>@endif</div>
@endsection
